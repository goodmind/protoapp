import { ProtobufService, ProtobufMessage } from '../interfaces';
export declare function generateImportsMessage(model: ProtobufMessage, primitives: string[]): string[];
export declare function generateImportsService(model: ProtobufService, primitives: string[]): string[];
