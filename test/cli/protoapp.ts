import * as test from 'tape'
import { main } from '../../src/cli/protoapp'

test('protoapp', t => {
  t.test('createEntity', t => {
    // side effects

    t.end()
  })

  t.test('main', t => {
    // side effects

    t.end()
  })

  t.end()
})
